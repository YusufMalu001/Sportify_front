import React from "react";
import { Container, Typography } from "@mui/material";

const Tournaments = () => {
    return (
        <Container>
            <Typography variant="h4" gutterBottom>
                Tournaments
            </Typography>
            {/* You can add Tournament Form and List similar to the above components */}
        </Container>
    );
};

export default Tournaments;
