import React from "react";
import { Container, Typography } from "@mui/material";

const Matches = () => {
    return (
        <Container>
            <Typography variant="h4" gutterBottom>
                Matches
            </Typography>
            {/* You can add Match Form and List similar to the above components */}
        </Container>
    );
};

export default Matches;
